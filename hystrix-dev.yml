info:
  component: Hystrix Turbine Dashboard
server:
  hostname: ${VHOSTNAME:localhost.shinigami.io}
  port: 1234
security:
  basic:
    enabled: true
  user:
    name: hystrix
    password: password
applications: KUBE-SERVER,KUBE-FRONT,ZUUL
turbine:
  clusterNameExpression: new String("default")
  appConfig: ${applications}
